package com.example.budgetr.ui.theme

import androidx.compose.ui.graphics.Color


// Background and Surface
val Surface = Color(0xFFF5F5F5) // Light background for main content areas
val BackgroundElevated = Color(0xFF1A237E) // Deep blue for elevated surfaces like the app bar

// Text colors
val TextPrimary = Color(0xFF000000) // Pure black for primary text on dark backgrounds
val LabelSecondary = Color(0xFF424242) // Dark gray for secondary text, providing enough contrast

// Action and Highlight colors
val Primary = Color(0xFF42A5F5) // Brighter blue for primary actions and highlights
val Destructive = Color(0xFFFF5252) // Bright red for destructive actions

// Miscellaneous colors
val SystemGray04 = Color(0xFF1A237E) // Neutral gray for system elements
val DividerColor = Color(0xFFE1E1E1) // Slightly darker gray for dividers to pop a bit more
val TopAppBarBackground = BackgroundElevated // Deep blue for top app bar to match elevated backgrounds
val FillTertiary = Color(0xFF777A94) // A different shade of blue for tertiary fills

// Color constants used in the app's theme
val Surface = Color.Black
val TextPrimary = Color.White
val Destructive = Color(0xFFFF453A) // Represents destructive actions or errors
val Primary = Color(0xFF0A84FF) // Represents primary elements, e.g., buttons

val SystemGray04 = Color(0xFF3A3A3C) // A shade of gray used in the system UI
val BackgroundElevated = Color(0xFF1C1C1E) // Background color for elevated content
val LabelSecondary = Color(0xFF8E8E93) // Secondary label color for text
val DividerColor = Color(0x20787880) // Color for dividers with reduced opacity
val TopAppBarBackground = Color(0x5E1D1D1D) // Top app bar background color
val FillTertiary = Color(0x18767680) // Tertiary fill color with reduced opacity

